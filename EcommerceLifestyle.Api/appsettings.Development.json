{
  // ─────────────────────────────────────────────────────────────────────
  //  appsettings.Development.json -- DEPLOYED "Development" branch only.
  //
  //  Loaded ON TOP of appsettings.json when ASPNETCORE_ENVIRONMENT=Development.
  //  Only the keys present here override the base file -- everything else
  //  (CORS, JWT issuer/audience, Logging, Features, ...) inherits from
  //  appsettings.json.
  //
  //  Activate on Railway:  Service -> Variables -> add
  //      ASPNETCORE_ENVIRONMENT = Development
  //  on the Railway service that auto-deploys the `Development` branch.
  // ─────────────────────────────────────────────────────────────────────
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    // Railway internal hostname -- only resolves inside Railway's private network.
    "Default": "Server=mysql.railway.internal;Port=3306;Database=railway;User=root;Password=HhnqXZDOOddrrzpJMfyToKRDeCZPGQjO;TreatTinyAsBoolean=true"
  },
  "Oidc": {
    // Google / GitHub OAuth apps registered for the deployed Development URL.
    // Authorized redirect URIs must point at the deployed frontend, e.g.
    //   https://dev.your-frontend.example/auth/google/callback
    //   https://dev.your-frontend.example/auth/github/callback
    "Google": {
      "ClientId": "REPLACE_WITH_DEV_GOOGLE_CLIENT_ID",
      "ClientSecret": "REPLACE_WITH_DEV_GOOGLE_CLIENT_SECRET"
    },
    "GitHub": {
      "ClientId": "REPLACE_WITH_DEV_GITHUB_CLIENT_ID",
      "ClientSecret": "REPLACE_WITH_DEV_GITHUB_CLIENT_SECRET"
    }
  }
}
